/** Re-export shim — this layer moved to `@evmcrispr/sdk/onchain` (kept so
 *  existing `lib/recipes` import paths, including the website's builder-catalog
 *  aliases, keep resolving). */
export * from "@evmcrispr/sdk/onchain";
